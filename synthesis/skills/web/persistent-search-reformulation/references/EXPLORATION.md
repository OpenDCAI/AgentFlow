# Phase 1: Environment Exploration Guide
* **Exploration Strategy**:
  - Initiate search sequences deriving from the most oblique, multi-dimensional clue bundles available rather than predictable direct entity lookups. Issue foundational broad retrieval probes to ascertain the general category of relevant documentation, systematically adjusting the semantic direction based on the nature of preliminary results. If the first broad keyword perfectly matches the final expected answer within a raw AI-generated snippet, immediately discard the seed task and reboot the traversal using an exponentially tougher constraint.
  - Implement tactical Fallback Silo Identification when encountering systemic unrecoverable environmental blockers organically on the open web. Should the primary target URL deploy hostile access blocks like 403 Forbidden screens, impenetrable paywalls, or bot-detection CAPTCHAs, the agent must quickly diagnose this barrier and reformulate the prompt toward third-party aggregators. Discovering crucial data is completely blocked on an official domain forces the immediate tactical pivot to cached regulatory repositories, search archives, or secondary review ecosystems.
  - Iteratively pivot the conceptual search taxonomy across differing semantic axes subsequent to identifying low-yield or corrupted search chains. Transit logically from searching entity-centric nomenclature to querying role-specific or time-bounded keywords whenever distinct failures (such as zero hits, hallucinated matches, or overly broad sweeps) degrade the workspace. A venue-focused query yielding thousands of useless peripheral entries requires abandoning the platform-centric vocabulary and entirely switching pursuit toward localized textual metadata to squeeze the candidate pool.
  - Favor trajectories deliberately exposing completely resolvable operational dead ends or frustrating blockages before securing final validation. The pathway must demonstrably track the system subscribing to an initially plausible theoretical route, confronting glaringly contradictory facts or system errors, and instituting an aggressively reframed query angle based entirely on this acquired friction. Trusting a basic public wiki that redirects endlessly must invoke the agent's critical realization of the failure, prompting targeted verification searches elsewhere.
* **Target Trajectory Profile**:
  - The finalized pathway absolutely must encompass multiple materially disparate search formulations possessing entirely altered structural parameters. Superficial cosmetic adjustments like punctuation shifts or plural adjustments fail the diagnostic threshold entirely. A sequence shifting gracefully from temporal limits to precise relational tracking definitively validates the complex search objective.
  - The trajectory maps progressive information gain and obstacle recovery methodically throughout the iteration chain. Each new formulation actively strips away false positives, conquers an explicit technical barricade, or effectively translates an implicit constraint into a strict data pillar. Encapsulating a scenario neutralizing a major login-wall blocker directly through discovering an unsanctioned mirrored archive defines superior exploration.
  - A robust execution demands at least one explicitly documented backtrack away from failed premises or blocked networks. The application must pinpoint an assumption generating a useless loop, comprehensively log that insight internally, and execute a fresh query from an unprecedented angle. Overlooking a paywalled news publication immediately requires navigating away toward an open-access press pipeline.
  - Ultimate evidence realization heavily pivots exclusively upon executing the complete reformulated logic sequence rather than baseline initial queries. Documentation unambiguously illustrates that preliminary requests simply resulted in dead ends, security traps, or massive ambiguity. Should subsequent checks reveal the earliest query incidentally captured the entire answer spectrum, the test scenario structurally collapses under a lack of severity.
