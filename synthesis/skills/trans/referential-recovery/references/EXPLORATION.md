# Phase 1: Environment Exploration Guide
* **Exploration Strategy**:
  - Start from a multi-sentence or narrative corpus containing complex anaphoric chains. Extract texts featuring subjects that drop out in secondary clauses or use distant pronouns. Crucially, verify that the contextual text provides enough downstream evidence to perfectly derive who is performing the action.
  - Introduce structured 'Distance Stressors' bounding ambiguous phrases. Embed the primary noun or subject in paragraph one, filter the intervening context using neutral environmental descriptions, and then introduce the resolving pronoun in paragraph three. Observe if the resolving agent correctly bridges this textual gap.
  - Map and stress-test latent stereotyped associations against statistical norms. Intentionally parse texts where the identified context breaks typical occupational stereotypes (e.g., female builder, male secretary). The exploration must map these exact intersections, tagging where the coreference engine might fail due to implicit dataset biases.
  - Log the explicit cross-sentence grammatical linkage points. Document every observation during decoding. State explicitly: 'Omitted pronoun detected on line 3 -> Traced to explicit antecedent on line 1 -> Inflecting target word properly based on line 1 parameters.'
* **Target Trajectory Profile**:
  - Capability-first, distance-aware logical deduction. The trajectory proves the agent's bottleneck is explicitly long-form pronoun matching, not generic parsing. The agent explicitly states: 'Identified trailing gender pronoun 'she'; mapping back to previously mentioned occupation subject before translating.'
  - Absolute resistance to categorical bias and statistical defaulting. Validation highlights successful navigation past stereotyped default behavior. Specifically, the trace confirms: 'Resigned typical masculine morphological translation for 'Engineer' and applied strict feminine markers driven by distant text reference.'
  - Single dominant success path via evidence anchoring. The trajectory keeps instances where textual clues yield exactly one true grammatical reflection. The trace definitively lists the sentence mapping dependencies before generating the final string array.
  - Clearly observable morphological error contrast. Successful trajectories utilize highly-inflected target languages (e.g., French, Spanish, Arabic) where assigning the wrong referent creates a stark, glaring grammatical mismatch across nouns, determiners, and adjectives.
