# Phase 3: Data Synthesis Instructions
* **Question Generation Rules**:
  - Cross-Version Evolution Prompting: Actively design syntactic instructions leveraging linguistic phrases explicitly referencing hypothetical or finalized system updates to push the agent toward a verification mindset. Employ targeted layman expressions mimicking 'The app UI updated overnight, can you locate my payment history now?' or 'Find the precise setting applying my legacy process to this newly launched layout.' This rigorously forces the architecture mapping sequence directly into primary consideration.
  - Platform-Agnostic Goal Structuring: Phrase task outcomes anchoring purely on functional deliverables fundamentally identical across completely independent deployment platforms while bypassing rigid OS-specific jargon. Formulate the core instruction strictly referencing the target output, deliberately avoiding limiting vocabulary like 'Tap the Android drawer' or 'Right-click the browser pane.' Instructing 'Source my historical orders using this interface' demands the agent intrinsically figure out the local navigation taxonomy.
  - Functional Equivalent Clue Hinting: Strategically inject non-technical, localized contextual clues bridging historical application versions with the newly discovered interface hierarchy. Leverage approachable analogies like 'Keep an eye out for the section handling recurring deliveries' to scaffold intermediate reasoning connections. Crafting questions identifying 'Navigate this unknown marketplace replicating the functionality of the Flash Sale pane from our classic app' flawlessly tests conceptual portability.
  - Target Ambiguity Verification Integration: Assemble prompts directly requiring the agent confirm environmental metadata or contextual parameters dynamically prior to finalizing executing actions inside untested bounds. Use phrasing emphasizing 'Once you verify this is the web portal version' or 'Double check the layout before pressing complete' establishing inherent prudence parameters. A sterling challenge reads: 'Scan the version build first; if it is the legacy app, grab the code there, otherwise extract it via the new side menu.'
  - Controlled Prior-Entrapment Distractor Insertion: Tactically embed a convincingly accurate historic visual decoy aligning perfectly with the agent's theoretical stale prior, yet remaining operationally inert in the active runtime. Actively draft the trajectory to highlight an obsolete menu entry visually mimicking the expected target but logically disconnected from the true solution. Placing an impotent 'Account Settings' tile on the homepage while the true target moved into an isolated 'Privacy Dashboard' ensures supreme discipline grading.
  - Unambiguous Final Endpoint Binding: Force the conclusive response text to rigorously abide by stringent output formatting deriving exclusively from successfully conquering the unfamiliar hierarchical pathway. Eliminate generic descriptive adjectives from the question, preventing the agent from guessing the outcome via statistical language probabilities. Mandating a short alphanumeric extraction native only to the successfully mapped target sub-menu ensures evaluation integrity persists at 100% reliability.
* **Expected Output Format**: Output the QA pairs you generate in the following JSON format. Please construct the trajectory section based on your real exploration trajectory.
{
  "question": "...",
  "answer": "...",
  "trajectory": [
    {"step": 1, "observation": "...", "action": "..."},
    {"step": 2, "observation": "...", "action": "..."}
  ]
}
