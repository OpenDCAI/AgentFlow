# Phase 1: Environment Exploration Guide
* **Exploration Strategy**:
  - Implement 'Object-Centric Slot Decomposition' to segment the video into discrete entity vectors rather than a single global representation. First, scan the scene to identify persistent clusters of pixels that move as a unit, such as a robot arm or a specific block, and assign a unique identifier to each 'slot'. A concrete instance involves isolating the 'red cube' as a dedicated entity slot even when it is surrounded by identically shaped but differently colored distractors, ensuring tracking remains locked onto the target physics.
  - Execute 'Multi-Entity Interaction Mapping' to record the spatial dependencies between the primary actor and the target object throughout the sequence. You must document the exact frame where 'Entity-A' (e.g., the hand) enters the proximity threshold of 'Entity-B' (the cube) to establish physical engagement. For example, log a 'contact' event only when the pixel boundaries of the end-effector and the target block overlap, actively ignoring any movements near adjacent distractor blocks.
  - Perform 'Visual Randomization Stress-Testing' by intentionally seeking segments where object colors, sizes, or background textures shift across cuts to test robustness. First, identify the 'Invariant Attributes' of the target entity, such as its shape or relative location, and verify if these attributes remain recoverable when contextual colors change. An edge case involves confirming that a 'red cube' is still correctly identified even if the table color changes from black to white, which often causes 'holistic' models to fail.
  - Conduct 'Adversarial/Absence Grid Auditing' to generate explicit geographical negation maps that confirm target non-existence, thereby limiting confident hallucinations. You should establish defined visible zones, such as shelves or distinct background areas, and deploy focused negative confirmation checks documenting the zero-presence of the requested artifact. For example, explicitly noting that 'the keys possess 0% visibility overlap upon the mapped floor vectors' establishes rigid hallucination defense and proves active searching.
  - Initiate 'Long-Tail Constraint & Anchor-Fact Scanning' deploying intensified resolution framing to retrieve highly localized text scripts, symbols, or numerical displays that serve as variables for external knowledge queries. You should prioritize granular pixel assessment by cropping into signage, equipment labels, or background monitors before analyzing their semantic value. Retrieving these accurately demands zooming explicitly onto a distant park sign to extract the name 'Joliet' before passing that token to a downstream research or mathematical script.
* **Target Trajectory Profile**:
  - A good trajectory must satisfy 'Object-Centric Determinacy' where every retrieved detail is anchored to a specifically segmented entity rather than a general background gist. First, verify that your trajectory notes provide a strict 1-to-1 mapping between the 'Entity ID' and the observed state-change. For example, if a block moves, the trajectory must cite the 'Red Cube Slot' specifically, proving the answer isn't based on a generalized 'something moved' detection block.
  - The explored path must demonstrate 'Robustness to Distractor Variability' where the target remains tracked despite changes in nearby object attributes or high-salience noise. You must verify that the agent's focus does not erroneously jump to a distractor if the distractor's color or motion suddenly becomes more visually dominant. This ensures that the retrieved geometric details remain 'Identity-Locked' to the correct functional entity, avoiding confusion during cluttered interactions.
  - Ensure the path establishes 'Sequential Interaction Consistency' where the mapped physical relationship between two entities remains objectively plausible over time. First, document the 'Approach Phase' where the actor moves toward the object, verifying that a successful grasp is preceded by proper spatial coordinate alignment. If the robot's coordinates and the cube's coordinates never mathematically intersect during the trajectory, you must flag the trajectory as a tracking error.
  - The explored path must incorporate definitive 'Verification Fail-safes' to prove complete spatial frustum evaluation, rendering 'absent' confirmations as objective truths rather than lazy omissions. You must ensure trajectory notes provide visual testimonies logging continuous, unobstructed perspectives across the logical drop zones or tables. Verifying clear sightlines that monitor the complete operational surface locks in the mathematical 'absence' designation, preventing AI from guessing based on occlusion.
