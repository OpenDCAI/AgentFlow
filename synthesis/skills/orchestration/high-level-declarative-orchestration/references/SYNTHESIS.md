# Phase 3: Data Synthesis Instructions
* **Question Generation Rules**:
  - Formulate 'Multi-Phase Architectural Objectives' where the terminal goal naturally demands navigating across several disconnected logical partitions, infrastructure boundaries, or topological segments. The target prompt should ask for a sophisticated outcome, such as 'Reconstruct our entire backup database from the partitioned legacy system to the active cloud tier,' enforcing sequential macro-action planning. This entirely prevents simple single-shot API interactions from accidentally passing as high-level long-horizon coordination operations.
  - Design intricate 'Topology Interruption Traps' where the most straightforward intuitive macro-path is intentionally broken or barricaded, compelling the system to discover a multi-staged alternative pivot point. You might make the primary storage gateway inaccessible natively, testing whether the orchestrator consults the environment state service to discover an obscure staging cache mechanism. This perfectly audits the module's capacity to engage in dynamic graph traversal processing utilizing non-obvious, declarative chain linkages.
  - Weave 'Compound Asset Prerequisite Loops' forcing the system to combine disparate properties from completely isolated early stages to successfully unlock the final objective node. Construct a scenario where initializing a specialized data pipeline requires simultaneous access to an encryption key generated at Phase I and a network route derived dynamically from Phase III. This strictly punishes agents failing to map macro-discoveries into the persistent auxiliary state database essential for cross-horizon asset consolidation.
  - Leverage 'Layman Workflow Colloquialisms' inside the prompt constraints to aggressively test the translation layer mapping disorganized human ambitions onto rigorous declarative logic paths. Command the orchestrator using broad phrasing like 'Link up the different servers until the process flows smoothly' or 'Manage the deployment without bothering me with the coding details.' This explicitly validates the system's capability converting non-technical strategic demands directly into the correct formalized framework APIs governing the multi-agent execution backbone.
* **Expected Output Format**: Output the QA pairs you generate in the following JSON format. Please construct the trajectory section based on your real exploration trajectory.
{
  "question": "...",
  "answer": "...",
  "trajectory": [
    {"step": 1, "observation": "...", "action": "..."},
    {"step": 2, "observation": "...", "action": "..."}
  ]
}
