# Phase 3: Data Synthesis Instructions
* **Question Generation Rules**:
  - Formulate 'Resource-Intensive, Long-Horizon' prompts demanding speed optimization to force the orchestrator into batching tools safely behind autonomous milestones, ensuring the baseline execution heavily utilizes 'Step-Skip' validations.
  - Design scenarios featuring dual-layered faults: plant an easily rectifiable 'Transient Parameter Error' early in the pipeline requiring a localized inner-loop patch, followed heavily by a 'Systemic Data Blockage' ultimately requiring a global outer-loop structural revision.
  - Include a 'Semantic Contamination Trap' wherein a superficially successful query retrieves duplicated backend records. Triggering a downstream math malfunction directly forces the orchestrator to debug across multiple dependency phases to protect overarching data flow coherence.
  - Utilize 'Layman Efficiency Directives' (e.g., 'only verify the major milestones' or 'protect the final analysis from any upstream snowballing errors') bridging colloquial human intent into the strict tiered Progressive-Validation framework.
* **Expected Output Format**: Output the QA pairs you generate in the following JSON format. Please construct the trajectory section based on your real exploration trajectory.
{
  "question": "...",
  "answer": "...",
  "trajectory": [
    {"step": 1, "observation": "...", "action": "..."},
    {"step": 2, "observation": "...", "action": "..."}
  ]
}
