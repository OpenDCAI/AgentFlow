# 配置分级体系

Sandbox HTTP Service 采用三级配置体系，实现从静态到动态、从全局到局部的灵活配置管理。

---

## 📊 配置层级总览

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                              配置金字塔                                         │
│                                                                                 │
│                           ┌─────────────┐                                       │
│                           │  L3: 运行时  │  ← 用户传入，最高优先级               │
│                           │   (动态)     │     warmup/reinitialize 参数         │
│                           └──────┬──────┘                                       │
│                                  │                                              │
│                        ┌─────────┴─────────┐                                    │
│                        │  L2: 资源默认配置  │  ← 后端注册时设置                   │
│                        │    (半静态)       │     resources.xxx.config           │
│                        └─────────┬─────────┘                                    │
│                                  │                                              │
│              ┌───────────────────┴───────────────────┐                          │
│              │          L1: 服务器配置               │  ← 启动时加载，不可变      │
│              │            (静态)                     │     configs/*.json        │
│              └───────────────────────────────────────┘                          │
│                                                                                 │
└─────────────────────────────────────────────────────────────────────────────────┘
```

---

## 🏛️ Level 1: 服务器配置（静态）

### 📁 文件位置
```
sandbox/configs/
├── dev_minimal.json    # 开发-最小
├── dev_full.json       # 开发-完整
└── production.json     # 生产环境
```

### 🎯 用途
- 定义服务器监听地址和端口
- 决定启用哪些后端资源
- 设置全局参数（TTL、日志级别等）

### 📝 配置结构
```json
{
  "server": {
    "host": "0.0.0.0",          // 监听地址
    "port": 8080,               // 端口号
    "title": "Sandbox Service", // 服务名称
    "session_ttl": 300,         // Session 默认存活时间（秒）
    "cleanup_interval": 60,     // 清理任务间隔（秒）
    "log_level": "INFO"         // 日志级别
  },
  
  "resources": { ... },         // → 进入 L2
  "warmup": { ... },            // 预热配置
  "security": { ... }           // 安全配置
}
```

### ⏰ 生效时机
- **加载时机**: 服务器启动时
- **生效范围**: 整个服务器实例
- **可否修改**: ❌ 不可修改（需重启服务器）

### 🔧 使用方式
```bash
# 命令行启动
python -m sandbox.server.config_loader configs/production.json

# Python 代码
from sandbox.server import create_server_from_config
server = create_server_from_config("configs/production.json")
server.run()
```

---

## 🔧 Level 2: 资源默认配置（半静态）

### 📁 配置位置
```json
// configs/production.json
{
  "resources": {
    "vm": {
      "enabled": true,              // 是否启用
      "backend_class": "...",       // 后端实现类
      "description": "...",         // 后端描述（可选）
      "config": {                   // ← L2 配置在这里
        "screen_size": [1920, 1080],
        "headless": false,
        "provider": "local"
      }
    }
  }
}
```

### 🎯 用途
- 为每种资源类型设置**默认**初始化参数
- 当用户不传入自定义配置时使用
- 定义资源的"出厂设置"

### 📋 各资源类型配置示例

| 资源类型 | 典型配置字段 | 说明 |
|---------|-------------|------|
| **vm** | `screen_size`, `headless`, `provider` | 虚拟机屏幕和运行模式 |
| **rag** | `model_name`, `top_k`, `device` | 检索模型和参数 |
| **browser** | `browser_type`, `viewport`, `timeout` | 浏览器类型和窗口 |
| **websearch** | `search_engine`, `max_results`, `api_key` | 搜索引擎配置 |
| **code_executor** | `runtime`, `timeout`, `memory_limit` | 代码执行环境 |

### ⏰ 生效时机
- **加载时机**: 后端注册到服务器时
- **生效范围**: 该资源类型的所有 Session（作为默认值）
- **可否修改**: ❌ 服务器运行期间不可修改

### 🔄 存储流程
```python
# 后端注册时自动存储到 ResourceRouter
server.register_resource_type(
    resource_type="vm",
    initializer=backend.initialize,
    cleaner=backend.cleanup,
    default_config={"screen_size": [1920, 1080], ...}  # ← L2 配置
)

# 存储位置
ResourceRouter._resource_configs["vm"] = {"screen_size": [1920, 1080], ...}
```

---

## 🚀 Level 3: 运行时配置（动态）

### 📁 配置来源
用户在调用 API 时传入的参数

### 🎯 用途
- 为**特定 Session** 定制初始化参数
- 覆盖 L2 默认配置
- 支持动态调整资源行为

### 📝 使用场景

#### 场景 1: warmup 预热
```python
# 方式 A: 使用默认配置（L2）
await sandbox.warmup(["vm", "rag"])

# 方式 B: 使用自定义配置（L3 覆盖 L2）
await sandbox.warmup({
    "vm": {"screen_size": [1024, 768], "headless": True},
    "rag": {"top_k": 20, "model": "bge-large"}
})
```

#### 场景 2: create_session 显式创建
```python
# 使用自定义配置
await client.create_session("vm", {
    "screen_size": [2560, 1440],
    "provider": "cloud"
})
```

#### 场景 3: reinitialize 重新初始化
```python
# 更换配置，重建资源
await sandbox.reinitialize("rag", {
    "model": "new-model-v2",
    "top_k": 50,
    "rerank": True
})
```

#### 场景 4: 自动创建（无 L3 配置）
```python
# 直接执行，自动使用 L2 默认配置
await sandbox.execute("vm:screenshot", {})
```

### ⏰ 生效时机
- **加载时机**: Session 创建时
- **生效范围**: 单个 Session
- **可否修改**: ✅ 可通过 `reinitialize` 更新

---

## 🔀 配置优先级与合并规则

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                            配置优先级（高 → 低）                                │
│                                                                                 │
│   L3: 运行时配置          →  用户传入的 config 参数                             │
│          ↓                                                                      │
│   L2: 资源默认配置        →  resources.xxx.config                              │
│          ↓                                                                      │
│   L1: 服务器配置          →  server.* 字段                                      │
│                                                                                 │
└─────────────────────────────────────────────────────────────────────────────────┘
```

### 合并逻辑（ResourceRouter 第 173 行）
```python
# 优先使用用户传入的 config，否则使用默认配置
init_config = config or self._resource_configs.get(resource_type, {})
```

### 具体示例

```python
# L2 默认配置
resources.vm.config = {"screen_size": [1920, 1080], "headless": False}

# 场景 1: 用户不传配置
await sandbox.warmup(["vm"])
# → 使用 L2: {"screen_size": [1920, 1080], "headless": False}

# 场景 2: 用户传入部分配置
await sandbox.warmup({"vm": {"headless": True}})
# → 使用 L3: {"headless": True}  ⚠️ 注意：完全替换，不是合并！

# 场景 3: 用户传入完整配置
await sandbox.warmup({"vm": {"screen_size": [1024, 768], "headless": True}})
# → 使用 L3: {"screen_size": [1024, 768], "headless": True}
```

---

## 📊 配置对照表

| 层级 | 名称 | 文件/来源 | 作用范围 | 修改时机 | 典型字段 |
|------|------|----------|---------|---------|---------|
| **L1** | 服务器配置 | `configs/*.json` | 整个服务器 | 启动前 | `host`, `port`, `session_ttl` |
| **L2** | 资源默认配置 | `resources.xxx.config` | 资源类型 | 启动前 | `screen_size`, `model`, `top_k` |
| **L3** | 运行时配置 | API 参数 | 单个 Session | 运行时 | 同 L2，但可动态覆盖 |

---

## 🔄 完整配置流转示意

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│  1. 服务器启动                                                                  │
│     configs/production.json                                                     │
│            │                                                                    │
│            ▼                                                                    │
│     ConfigLoader.load()                                                         │
│            │                                                                    │
│            ├─→ server.host = "0.0.0.0"         (L1 生效)                        │
│            ├─→ server.port = 8080              (L1 生效)                        │
│            └─→ resources.vm.enabled = true     (决定加载 VM 后端)               │
│                                                                                 │
├─────────────────────────────────────────────────────────────────────────────────┤
│  2. 后端加载                                                                    │
│     server.load_backend(VMBackend())                                            │
│            │                                                                    │
│            ▼                                                                    │
│     server.register_resource_type(                                             │
│         "vm",                                                                   │
│         default_config = resources.vm.config   (L2 存储)                        │
│     )                                                                           │
│            │                                                                    │
│            ▼                                                                    │
│     ResourceRouter._resource_configs["vm"] = {                                  │
│         "screen_size": [1920, 1080],                                           │
│         "headless": false                                                       │
│     }                                                                           │
│                                                                                 │
├─────────────────────────────────────────────────────────────────────────────────┤
│  3. Session 创建                                                                │
│                                                                                 │
│     ┌──────────────────────────┐    ┌──────────────────────────┐               │
│     │ warmup(["vm"])           │    │ warmup({"vm": {...}})    │               │
│     │ (无用户配置)              │    │ (有用户配置)              │               │
│     └────────────┬─────────────┘    └────────────┬─────────────┘               │
│                  │                               │                              │
│                  ▼                               ▼                              │
│     config = L2 默认配置              config = L3 用户配置                       │
│     {"screen_size": [1920,1080]}     {"screen_size": [1024,768]}               │
│                  │                               │                              │
│                  └───────────────┬───────────────┘                              │
│                                  │                                              │
│                                  ▼                                              │
│                    backend.initialize(worker_id, config)                        │
│                                  │                                              │
│                                  ▼                                              │
│                    Session 创建完成，config 存储在 session_info                  │
│                                                                                 │
├─────────────────────────────────────────────────────────────────────────────────┤
│  4. 重新初始化（可选）                                                           │
│     await sandbox.reinitialize("vm", new_config)                                │
│            │                                                                    │
│            ├─→ destroy_session("vm")           销毁旧 Session                   │
│            │        │                                                           │
│            │        ▼                                                           │
│            │   backend.cleanup()                                                │
│            │                                                                    │
│            └─→ create_session("vm", new_config) 创建新 Session                  │
│                     │                                                           │
│                     ▼                                                           │
│                backend.initialize(worker_id, new_config)                        │
│                                                                                 │
└─────────────────────────────────────────────────────────────────────────────────┘
```

---

## ✅ 最佳实践

### 1. L1 服务器配置
- 生产环境使用独立的 `production.json`
- 敏感信息使用 `${ENV_VAR}` 环境变量
- 只启用需要的资源，减少内存占用

### 2. L2 资源默认配置
- 设置合理的"开箱即用"默认值
- 考虑大多数用户的使用场景
- 文档化每个配置字段的含义

### 3. L3 运行时配置
- 仅在需要定制时传入
- 完整传入所需字段（非合并模式）
- 使用 `reinitialize` 动态调整

