{
  "$schema": "L1 - 服务器配置",
  "$comment": "服务器级别默认配置，host/port 由 Sandbox(server_url=...) 指定",

  "title": "Sandbox HTTP Service",
  "description": "Multi-resource Sandbox Service",
  
  "session_ttl": 300,
  "cleanup_interval": 60,
  "log_level": "INFO",
  
  "cors": {
    "enabled": true,
    "allowed_origins": ["*"]
  }
}

