{
  "$schema": "L2 - Bash 资源默认配置",
  "$resource_type": "bash",
  
  "enabled": true,
  "description": "Bash 终端后端（Mock）- 命令行交互",
  "backend_class": "sandbox.server.backends.resources.bash.BashBackend",
  
  "config": {
    "shell": "bash",
    "shell_type": "local",
    "cwd": "/tmp",
    "timeout": 30
  }
}

