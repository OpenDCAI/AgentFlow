{
  "$schema": "L2 - WebSearch 资源默认配置",
  "$resource_type": "websearch",
  
  "enabled": true,
  "description": "网络搜索工具（Mock）- 无需 API Key",
  
  "config": {
    "max_results": 10,
    "timeout": 30,
    "default_region": "cn"
  }
}
