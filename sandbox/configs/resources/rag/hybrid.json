{
  "$schema": "L2 - RAG 混合检索配置",
  "$resource_type": "rag",
  "$extends": "default.json",
  
  "description": "RAG 混合检索后端 - E5 + BM25",
  "backend_class": "sandbox.server.backends.resources.rag.RAGBackend",
  
  "config": {
    "model_name": "${RAG_MODEL_NAME:-intfloat/e5-base-v2}",
    "device": "cuda",
    "default_top_k": 10
  }
}

