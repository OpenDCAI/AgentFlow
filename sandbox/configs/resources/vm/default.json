{
  "$schema": "L2 - VM 资源默认配置",
  "$resource_type": "vm",
  
  "enabled": true,
  "description": "虚拟机交互后端（Mock）- 支持截图、点击、键盘输入等",
  "backend_class": "sandbox.server.backends.resources.vm.VMBackend",
  
  "config": {
    "screen_size": [1920, 1080],
    "connection_type": "local",
    "headless": false,
    "host": null,
    "port": null
  }
}
