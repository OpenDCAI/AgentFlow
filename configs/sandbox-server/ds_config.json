{
  "server": {
    "url": "http://127.0.0.1:18890",
    "port": 18890,
    "session_ttl": 300
  },
  "apis": {
    "ds": {
      "_comment_required": "seed_path is provided as a tool-call argument (not configured here)",
      "_comment_fallback": "Default fallback values: ds_max_csv / ds_summary_max_chars",
      "ds_max_csv": 100,
      "ds_summary_max_chars": 12000
    }
  }
}

